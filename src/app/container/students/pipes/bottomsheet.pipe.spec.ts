import { BottomsheetPipe } from './bottomsheet.pipe';

describe('BottomsheetPipe', () => {
  it('create an instance', () => {
    const pipe = new BottomsheetPipe();
    expect(pipe).toBeTruthy();
  });
});

import { AppLoadingDirective } from './app-loading.directive';

describe('AppLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = {};
    expect(directive).toBeTruthy();
  });
});

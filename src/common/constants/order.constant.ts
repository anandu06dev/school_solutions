export enum Order {
    ASC = 'ASC',
    DESC = 'DESC',
}

export enum JoinProjection {
    STUDENT_DETAILS = 'studentDetails',
    SIBLINGS = 'siblings',
    PARENTS = 'parents',
    FEES = 'fees',
    BUS_ROUTE_DETAILS = 'busRouteDetails',
    ADDRESSES = 'addresses',
}
